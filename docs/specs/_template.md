# <feature name>

## Goal
What should exist after this feature ships, and why it matters. 1-3 sentences.

## Non-goals
What this explicitly does NOT cover (scope boundary — prevents scope creep during implementation).

## Background / context
Relevant existing behavior, files, or constraints an implementer needs to know before touching code.

## Requirements
Numbered, concrete, testable statements of what the system must do.

1. ...
2. ...

## Acceptance criteria
How "done" is verified. Prefer checks a machine can run (a specific test, a specific command's output) over subjective judgment.

- [ ] ...
- [ ] ...

## Out of scope / follow-ups
Related work deliberately deferred to a later feature.

## Open questions
Anything genuinely ambiguous that only a human (product owner) can resolve — not implementation details an engineer could reasonably decide alone. If this section is non-empty, the spec is NOT ready for implementation.
